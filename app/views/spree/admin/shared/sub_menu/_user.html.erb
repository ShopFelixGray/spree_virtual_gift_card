<ul id="sidebar-user" class="collapse nav nav-pills nav-stacked" data-hook="admin_user_sub_tabs">
  <%= tab :users %>
  <% if can?(:display, Spree::VirtualGiftCard) %>
    <%= tab :gift_cards, match_path: "/users/gift_cards" %>
  <% end %>
</ul>
